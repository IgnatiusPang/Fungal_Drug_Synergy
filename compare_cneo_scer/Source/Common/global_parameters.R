
base_dir    <- "/home/ignatius/PostDoc/2019/Fungal_Drug_Synergy"

## List of directories
compare_base_dir    <- file.path(base_dir, "compare_cneo_scer" )
compare_data_dir    <- file.path ( compare_base_dir, "Data" ) 
compare_source_dir  <- file.path( compare_base_dir, "Source")
compare_common_dir  <- file.path( compare_source_dir, "Common")  # Commonly used codes (e.g. parameters file)
compare_results_dir <- file.path( compare_base_dir, "Results")

# C. neoformans directories
cneo_base_dir    <- file.path( base_dir, "cneo_synergy" )
cneo_data_dir    <- file.path( cneo_base_dir, "Data" ) 
cneo_source_dir  <- file.path( cneo_base_dir, "Source")
cneo_common_dir  <- file.path( cneo_source_dir, "Common")  # Commonly used codes (e.g. parameters file)
cneo_results_dir <- file.path( cneo_base_dir, "Results")

# S. cerevisiae directories
scer_base_dir    <- file.path( base_dir, "scer_synergy" )
scer_data_dir    <- file.path( scer_base_dir, "Data" ) 
scer_source_dir  <- file.path( scer_base_dir, "Source")
scer_common_dir  <- file.path( scer_source_dir, "Common")  # Commonly used codes (e.g. parameters file)
scer_results_dir <- file.path( scer_base_dir, "Results")




